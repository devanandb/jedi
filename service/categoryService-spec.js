describe('categoryService', function() {

  beforeEach(module('jedi'));

  it('should ...', inject(function(categoryService) {

	//expect(categoryService.doSomething()).toEqual('something');

  }));

});